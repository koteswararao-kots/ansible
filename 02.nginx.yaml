- name: Installing and Run nginx
  hosts: mongodb
  become: yes

  tasks: 
  - name: Install nginx
    ansible.builtin.yum:
    state: installed
  - name: start nginx
    ansible.builtin.service:
    state: started


